#include<time.h>

vector<int> RandomSampling( vector<int> arr)
{
  int i = 0;
  vector<int>res(k);
  for(i = 0; i < k; i++)
  {
    res[i] = arr[i];
  }
  srand(time(NULL));
  
  for(; i < n ; i++)
  {
    int j = rand() % (i + 1);
    if(j < k)
      res[j] = arr[i];
  }
  return res;
}
