vector<int> countBits(int num) {
        vector<int> res(num+1, 0);
        int offset = 1;
        
        for(int i = 1; i < num+1; i++)
        {
            if(offset*2 == i)
                offset*= 2;
            res[i] = res[i - offset] + 1;
        }
        return res;
    }
